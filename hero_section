import Link from "next/link";

export function Hero() {
  return (
    <section className="relative pt-32 pb-20 px-4 overflow-hidden">
      {/* Background decoration */}
      <div className="absolute inset-0 -z-10">
        <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[600px] bg-gradient-to-b from-violet-100/60 to-transparent rounded-full blur-3xl" />
      </div>

      <div className="max-w-4xl mx-auto text-center">
        {/* Badge */}
        <div className="inline-flex items-center gap-2 bg-violet-50 text-violet-700 text-sm font-medium px-4 py-2 rounded-full mb-8 border border-violet-100">
          <span className="w-2 h-2 bg-violet-500 rounded-full animate-pulse" />
          Novo: Integração com IA generativa
        </div>

        {/* Headline */}
        <h1 className="text-5xl md:text-6xl font-bold text-gray-900 leading-tight mb-6">
          Automatize seu fluxo de trabalho com{" "}
          <span className="text-violet-600">inteligência</span>
        </h1>

        <p className="text-xl text-gray-500 mb-10 max-w-2xl mx-auto leading-relaxed">
          FlowSaaS conecta suas ferramentas, automatiza tarefas repetitivas e
          libera sua equipe para o que realmente importa.
        </p>

        {/* CTAs */}
        <div className="flex flex-col sm:flex-row gap-4 justify-center mb-16">
          <Link
            href="#contact"
            className="px-8 py-4 bg-gray-900 text-white font-semibold rounded-2xl hover:bg-gray-700 transition-colors text-lg"
          >
            Começar grátis — 14 dias
          </Link>
          <Link
            href="#features"
            className="px-8 py-4 bg-white text-gray-700 font-semibold rounded-2xl border border-gray-200 hover:border-gray-400 transition-colors text-lg"
          >
            Ver demo ▶
          </Link>
        </div>

        {/* Social proof */}
        <p className="text-sm text-gray-400">
          Sem cartão de crédito · Cancele quando quiser · +5.000 times usando
        </p>
      </div>
    </section>
  );
}
